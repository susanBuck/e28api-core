/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/tests*
.env
.env.backup
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
.env


# These files are generated by the build system and should be ignored
routes/generated-routes.php
database/migrations/9999*
config/permissions.php

# The following directories have their own .gitignore file 
# so that we can include the directory, but not its contents in the repository.
# * /app/Models/GeneratedModels
# * /app/Http/Controllers/GeneratedControllers
# * /app/Http/Requests/GeneratedRequests
# * /database/factories/GeneratedModels